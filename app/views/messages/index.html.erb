<h1>Mini Message Board</h1>
<% @messages.each do |message| %>
	<h3><%= message.title %></h3>
	<p><%= message.description %></p>
	<h5><%= link_to "Edit Posting", edit_message_path(message) %></h5>
	<h5><%= link_to 'Delete Posting', message, method: :delete %></h5>
<% end %>